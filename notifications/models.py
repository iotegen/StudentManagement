from django.db import models

from django.db import models
from students.models import Attendance  # Correct import from the 'students' app

# Define your notification models here, or add other related models
